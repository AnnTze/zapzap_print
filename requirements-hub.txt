# Hub only — runs on the machine hosting the dashboard.
# Deliberately no Pillow and no python-telegram-bot: the hub neither processes
# images nor talks to Telegram. A test asserts this stays true.
aiohttp>=3.9
python-dotenv>=1.0
